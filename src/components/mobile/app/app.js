import React from 'react';
import { Container, Header, View, Text } from 'native-base';

export default () => (
  <Container>
    <Header />
    <View>
      <Text>Hello</Text>
    </View>
  </Container>
);
